// Create a function to calculate the area of a rectangle given its width and height.

const height = 10;
const width = 20;

const areaCalulator = (height, width) => {
	return height * width;
};

const ans = areaCalulator(height, width);

console.log(ans);
